node_modules/
dist/
dist-pruebas/
cobertura/
package-lock.json
# La maqueta v0.1 esta congelada tal y como se publico.
maqueta/
# Documentacion de diseno: se escribe a mano, con sus tablas alineadas a criterio.
docs/
*.md
# El mundo se genera en forma canonica (una sola linea, claves ordenadas): darle formato
# romperia la comprobacion de que nadie lo edita a mano.
paquetes/mundo/datos/
# Los informes del banco los escribe el propio banco, byte a byte: no se reformatean.
herramientas/banco/informes/
